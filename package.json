{
  "name": "groselha-tokens",
  "version": "0.0.1",
  "description": "Tokens base architecture",
  "directories": {
    "dist": "dist"
  },
  "files": [
    "dist/",
    "LICENSE",
    "README.md"
  ],
  "devDependencies": {
    "theo": "^8.0.0-beta.2"
  },
  "scripts": {
    "web": "theo src/tokens.yml --transform web --format scss --dest dist",
    "ios": "theo src/tokens.yml --transform ios --format ios.json --dest dist",
    "android": "theo src/tokens.yml --transform android --format android.xml --dest dist"
  },
  "keywords": [
    "tokens",
    "design tokens",
    "design system"
  ],
  "author": "thiagokpelo",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/thiagokpelo/groselha-tokens.git"
  }
}
